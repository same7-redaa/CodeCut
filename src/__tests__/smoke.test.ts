import { describe, it, expect } from 'vitest'

// Simple smoke test to ensure the test runner is wired correctly

describe('smoke', () => {
  it('adds numbers', () => {
    expect(1 + 2).toBe(3)
  })
})
